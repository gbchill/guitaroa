// Import the 'Header' component from the specified path
import Header from "../Header";

// Define the main component for the index page
export default function IndexPage() {
    return (
        <div>
            index page here
        </div>
    );
}
